// loading.tsx removed — page.tsx renders demo jobs immediately with loading=false
// Kept as minimal passthrough to prevent Next.js from showing default loading UI
export default function JobsLoading() {
  return null
}
